name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint-test-build:
    runs-on: ubuntu-latest

    services:
      docker:
        image: docker:20.10.16
        options: --privileged
        ports:
          - 2375:2375

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest mypy black ruff docker

    - name: Lint with Ruff
      run: |
        ruff .

    - name: Type check with Mypy
      run: |
        mypy .

    - name: Format check with Black
      run: |
        black --check .

    - name: Run tests with Pytest
      run: |
        pytest --maxfail=5 --disable-warnings -v

    - name: Build Docker image
      run: |
        docker build -t jbbaillet85/potion-de-vie:latest .

    - name: Push Docker image
      if: github.ref == 'refs/heads/main'
      env:
        DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
        DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      run: |
        echo $DOCKER_PASSWORD | docker login -u $DOCKER_USERNAME --password-stdin
        docker push jbbaillet85/potion-de-vie:latest

    - name: Deploy
      if: github.ref == 'refs/heads/main'
      run: |
        echo "Déploiement automatique ici"
        # Remplace par ton script de déploiement, ex: ssh vers serveur, kubectl, etc.
